# Setup (Minimal)

- Keine MCP/Playwright‑Konfiguration erforderlich.
- Optional: Lokalen Preview starten, z. B.:

```bash
npx http-server .superdesign/staging -p 8000
```

- Empfohlen: Echte Bild‑URLs (Unsplash, placehold.co), CDNs für CSS/Icons.

Das war’s – direkt mit dem Workflow starten.
